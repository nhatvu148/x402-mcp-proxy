# Copy to .env.local (gitignored). Loaded by Taskfile's `dotenv`.
# Wallet inspection lives in ../solana-ops (task balance / stakes / tokens).

CLUSTER=devnet                 # devnet | mainnet-beta
KEYPAIR=payer.json             # the proxy's spending wallet (never committed)
MCP_URL=http://127.0.0.1:8080/mcp
